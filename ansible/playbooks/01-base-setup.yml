# Playbook 01 — Configuração base de todos os nós do laboratório.
#
# Executa em: todos os nós (k3s_cluster + nós físicos que participam do cluster)
# Idempotente: sim — seguro de re-executar a qualquer momento
#
# O que faz:
#   1. Atualiza pacotes do sistema (apt)
#   2. Instala pacotes base necessários para o cluster
#   3. Configura timezone para America/Sao_Paulo
#   4. Desativa swap (obrigatório para Kubernetes)
#   5. Carrega módulos de kernel necessários para o K8s
#   6. Aplica configurações sysctl para rede e Kubernetes
#   7. Cria o usuário labadmin com sudo sem senha
#   8. Endurece configuração SSH (sem login root, sem senha)
#
# Uso:
#   ansible-playbook -i inventory/hosts.yml playbooks/01-base-setup.yml
#   ansible-playbook -i inventory/hosts.yml playbooks/01-base-setup.yml --check --diff

---
- name: Configuração base dos nós do laboratório
  hosts: k3s_cluster
  become: true
  gather_facts: true

  handlers:
    - name: Recarregar sshd
      ansible.builtin.service:
        name: sshd
        state: reloaded

    - name: Recarregar sysctl
      ansible.builtin.command:
        cmd: sysctl --system
      changed_when: true

  tasks:

    # -------------------------------------------------------------------------
    # Atualização de pacotes
    # -------------------------------------------------------------------------

    - name: Atualizar cache do apt
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 3600
      tags: [packages]

    - name: Realizar upgrade de pacotes de segurança
      ansible.builtin.apt:
        upgrade: safe
        autoremove: true
        autoclean: true
      tags: [packages]

    - name: Instalar pacotes base
      ansible.builtin.apt:
        name: "{{ base_packages }}"
        state: present
      tags: [packages]

    # -------------------------------------------------------------------------
    # Timezone
    # -------------------------------------------------------------------------

    - name: Configurar timezone para America/Sao_Paulo
      community.general.timezone:
        name: "{{ system_timezone }}"
      tags: [timezone]

    # -------------------------------------------------------------------------
    # Swap — deve estar desativado para o Kubernetes funcionar corretamente
    # -------------------------------------------------------------------------

    - name: Desativar swap imediatamente (memória)
      ansible.builtin.command:
        cmd: swapoff -a
      changed_when: ansible_swaptotal_mb > 0
      tags: [swap]

    - name: Remover entradas de swap do /etc/fstab (permanente)
      ansible.builtin.replace:
        path: /etc/fstab
        regexp: '^([^#].*\s+swap\s+.*)$'
        replace: '# \1  # desabilitado pelo Ansible para Kubernetes'
        backup: true
      tags: [swap]

    - name: Desativar e mascarar serviço swap (systemd)
      ansible.builtin.systemd:
        name: "{{ item }}"
        enabled: false
        masked: true
        state: stopped
      loop:
        - swap.target
      failed_when: false
      tags: [swap]

    # -------------------------------------------------------------------------
    # Módulos de kernel para Kubernetes/CNI
    # -------------------------------------------------------------------------

    - name: Garantir arquivo de módulos persistentes
      ansible.builtin.file:
        path: /etc/modules-load.d/k8s.conf
        state: touch
        mode: "0644"
      changed_when: false
      tags: [kernel]

    - name: Configurar módulos de kernel para carregar no boot
      ansible.builtin.lineinfile:
        path: /etc/modules-load.d/k8s.conf
        line: "{{ item }}"
        state: present
      loop: "{{ kernel_modules_k8s }}"
      tags: [kernel]

    - name: Carregar módulos de kernel agora
      community.general.modprobe:
        name: "{{ item }}"
        state: present
      loop: "{{ kernel_modules_k8s }}"
      tags: [kernel]

    # -------------------------------------------------------------------------
    # Configurações sysctl para Kubernetes
    # -------------------------------------------------------------------------

    - name: Aplicar configurações sysctl para K8s
      ansible.posix.sysctl:
        name: "{{ item.key }}"
        value: "{{ item.value }}"
        sysctl_file: /etc/sysctl.d/99-k8s.conf
        reload: true
        state: present
      loop: "{{ sysctl_k8s_settings }}"
      tags: [sysctl]

    # -------------------------------------------------------------------------
    # Usuário labadmin
    # -------------------------------------------------------------------------

    - name: Criar grupo labadmin
      ansible.builtin.group:
        name: labadmin
        state: present
      tags: [users]

    - name: Criar usuário labadmin
      ansible.builtin.user:
        name: labadmin
        group: labadmin
        groups: [sudo]
        append: true
        shell: /bin/bash
        create_home: true
        state: present
        comment: "Usuário de administração do laboratório"
      tags: [users]

    - name: Criar diretório .ssh do labadmin
      ansible.builtin.file:
        path: /home/labadmin/.ssh
        state: directory
        owner: labadmin
        group: labadmin
        mode: "0700"
      tags: [users]

    - name: Configurar sudo sem senha para labadmin
      ansible.builtin.lineinfile:
        path: /etc/sudoers.d/labadmin
        line: "labadmin ALL=(ALL) NOPASSWD:ALL"
        create: true
        mode: "0440"
        validate: "visudo -cf %s"
      tags: [users]

    # -------------------------------------------------------------------------
    # Usuário agente-ia — conta de escopo reduzido para sessões de agente de IA
    # (Claude Code e afins), separada de labadmin. Ver docs/architecture.md
    # seção "Acesso de agentes de IA" e secrets/README.md (env.agent.enc.yaml).
    # Sudo restrito a comandos de diagnóstico read-only — qualquer operação de
    # escrita/mudança de estado exige o humano rodar via labadmin.
    # -------------------------------------------------------------------------

    - name: Criar grupo agente-ia
      ansible.builtin.group:
        name: agente-ia
        state: present
      tags: [users, agente-ia]

    - name: Criar usuário agente-ia
      ansible.builtin.user:
        name: agente-ia
        group: agente-ia
        shell: /bin/bash
        create_home: true
        state: present
        comment: "Conta de escopo reduzido para sessões de agente de IA (somente diagnóstico)"
      tags: [users, agente-ia]

    - name: Criar diretório .ssh do agente-ia
      ansible.builtin.file:
        path: /home/agente-ia/.ssh
        state: directory
        owner: agente-ia
        group: agente-ia
        mode: "0700"
      tags: [users, agente-ia]

    - name: Instalar chave pública SSH do agente-ia
      ansible.builtin.copy:
        dest: /home/agente-ia/.ssh/authorized_keys
        content: "{{ agente_ia_ssh_public_key }}\n"
        owner: agente-ia
        group: agente-ia
        mode: "0600"
      tags: [users, agente-ia]

    - name: Restringir sudo do agente-ia a comandos de diagnóstico (sem escrita)
      ansible.builtin.copy:
        dest: /etc/sudoers.d/agente-ia
        # Somente leitura/status: sem systemctl restart|stop|start, sem apt,
        # sem edição de arquivos, sem k3s kubectl (acesso ao cluster é via
        # ServiceAccount/RBAC do K8s, não via SSH+root).
        content: |
          Cmnd_Alias AGENTE_IA_CMDS = /usr/bin/systemctl status *, \
            /usr/bin/journalctl *, /bin/journalctl *, \
            /usr/bin/df *, /usr/bin/free *, /usr/bin/uptime, \
            /usr/sbin/crictl ps *, /usr/sbin/crictl logs *, \
            /usr/sbin/crictl inspect *, \
            /usr/bin/ss *, /usr/bin/ip *, /usr/bin/cat /var/log/*
          agente-ia ALL=(ALL) NOPASSWD: AGENTE_IA_CMDS
        owner: root
        group: root
        mode: "0440"
        validate: "visudo -cf %s"
      tags: [users, agente-ia]

    # -------------------------------------------------------------------------
    # Hardening SSH
    # -------------------------------------------------------------------------

    - name: Desabilitar login SSH como root
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        regexp: '^#?PermitRootLogin'
        line: 'PermitRootLogin no'
        state: present
        backup: true
      notify: Recarregar sshd
      tags: [ssh]

    - name: Desabilitar autenticação por senha no SSH
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        regexp: '^#?PasswordAuthentication'
        line: 'PasswordAuthentication no'
        state: present
      notify: Recarregar sshd
      tags: [ssh]

    - name: Habilitar autenticação por chave pública
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        regexp: '^#?PubkeyAuthentication'
        line: 'PubkeyAuthentication yes'
        state: present
      notify: Recarregar sshd
      tags: [ssh]

    - name: Configurar ClientAliveInterval para evitar sessões fantasma
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        regexp: '^#?ClientAliveInterval'
        line: 'ClientAliveInterval 120'
        state: present
      notify: Recarregar sshd
      tags: [ssh]

    # -------------------------------------------------------------------------
    # Verificação final
    # -------------------------------------------------------------------------

    - name: Verificar que swap está desativado
      ansible.builtin.assert:
        that:
          - ansible_swaptotal_mb == 0
        fail_msg: >
          ERRO: Swap ainda ativo em {{ inventory_hostname }}.
          Reinicie o host e re-execute o playbook.
        success_msg: "Swap desativado com sucesso em {{ inventory_hostname }}"
      # Roda no final após swapoff para confirmar
      tags: [verify, swap]
